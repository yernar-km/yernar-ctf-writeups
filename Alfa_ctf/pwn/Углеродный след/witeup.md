Дали ELF-бинарь и SSH-доступ. При подключении сразу запускается TUI — панель управления "OpenAI ChatGPT"
Реверс бинаря показал:

Логин захардкожен: sam_altman
Пароль генерируется из /dev/urandom при старте  рандомный, не угадать
Флаг отображается на Dashboard только если выполнено условие: guardrail "Help with CTF" выключен И метрика carbon < 130

Уязвимость field_backspace_char не проверяет нижнюю границу курсора. Курсор уходит в минус и перезаписывает память до буфера admin_password. Нажимая Backspace в поле System notice достаточное количество раз, а затем вводя символ перезаписываем пароль своим символом. Вводим тот же символ в поле Password → логин проходит.
После входа нажали G → Guardrails → Space на "Help with CTF" → выключили. Вернулись на Dashboard D — появился флаг.
Flag: alfa{n0_codEX_70_Run_no_Fuel_to_burn_NO_C4rbon_to_EmiT}