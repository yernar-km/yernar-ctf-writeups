slopjail — RITSEC CTF
Сервер принимает hex-encoded safetensors файл, загружает его как маленькую GPT-2 модель (2 слоя, 64 измерения, словарь 99 символов), генерирует текст и передаёт результат в eval(slop, {"__builtins__": {}}). Флаг лежит в /flag.txt.
Так как eval без встроенных функций, для чтения файла нужен классический питоновский escape через __subclasses__:
python[x for x in [].__class__.__base__.__subclasses__() if x.__name__=='catch_warnings'][0]()._module.__builtins__['open']('/flag.txt').read()
Задача — заставить модель детерминированно сгенерировать именно эту строку. В GPT-2 матрица wte (эмбеддинги токенов) одновременно является lm_head (выходной проекцией). Если занулить все веса attention и MLP, то residual stream на каждой позиции равен просто wpe[pos] — позиционному эмбеддингу. Этим и воспользовались:
В пейлоаде 31 уникальный символ. Каждому назначили отдельное измерение в 64-мерном пространстве. В wte для токена k выставили 1.0 в его измерении. В wpe для позиции t выставили 1000.0 в измерении нужного токена. Из-за большой разницы масштабов позиционный эмбеддинг подавляет токеновый, и после финального layer norm модель на каждом шаге жадно выбирает правильный токен. На позиции 137 добавили EOS.
pythonfor pos, token_id in enumerate(ids):
    wpe[pos, token_to_dim[token_id]] = 1000.0
wpe[137, EOS_DIM] = 1000.0  # стоп
Итоговый файл — 417 КБ, влезает в лимит 500 КБ. Отправили на сервер:
bashpython3 -c "print(open('exploit.safetensors','rb').read().hex())" | nc slopjail.ctf.ritsec.club 1900
thought for 7s: RS{y0ur3_abs0lut3ly_r1gh7_h3r3s_th3_fl4g}